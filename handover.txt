MULTIPLAYER AUDIT HANDOVER — 2026-03-13
========================================

Three audit agents ran across the full codebase. 12 code fixes were applied.
4 prefab fixes remain (must be done in Unity Editor).

FILES MODIFIED (already saved):
- Assets/Scripts/Interaction/EquipmentHandler.cs
- Assets/Scripts/Interaction/FragmentCarrier.cs
- Assets/Scripts/Interaction/CarryableDebris.cs
- Assets/Scripts/RayfireNetwork/TransformSyncBroadcaster.cs
- Assets/Scripts/Tools/Detonator.cs
- Assets/Scripts/UI/NetworkMenuUI.cs
- Assets/Scripts/CharacterController/NetworkPlayerSetup.cs

WHAT WAS FIXED IN CODE:
1. [CRITICAL] EquipmentHandler.LateUpdate — removed IsOwner guard so equipped items render at hold point on ALL clients (was invisible on remotes)
2. [CRITICAL] FragmentCarrier — added static HashSet<int> _claimedFragments to prevent two players carrying the same fragment simultaneously
3. [HIGH] EquipmentHandler.OnNetworkDespawn — server force-drops held tool on disconnect (prevents orphaned kinematic tools)
4. [HIGH] FragmentCarrier.OnNetworkDespawn — server force-drops carried debris on disconnect (prevents orphaned kinematic fragments)
5. [HIGH] TransformSyncBroadcaster — added _forceSyncIds HashSet; carried fragments bypass the kinematic skip so they stay visible on other clients during carry
6. [HIGH] Detonator.OnUnequip — nulls linkedBomb so a re-picked-up detonator is inert (prevents orphaned bomb state)
7. [HIGH] EquipmentHandler.ServerForceEquip — now drops existing equipment first (prevents ghost items)
8. [HIGH] NetworkMenuUI — full rewrite: shows "Connecting..." status, re-shows menu on disconnect/failure instead of leaving player stuck on blank screen
9. [MEDIUM] TransformSyncBroadcaster.ReceiveSnapshots — O(1) HashSet for _activeTargetIds.Contains (was O(n) causing 500k+ comparisons at 1000 fragments)
10. [MEDIUM] NetworkPlayerSetup — disables GroundDetector on non-owner instances (saves wasted sphere casts)
11. [MEDIUM] CarryableDebris.CanInteract — returns false for already-claimed fragments (no misleading prompt)
12. [MEDIUM] TransformSyncBroadcaster.PurgeStale — respects force-sync set so carried fragment entries aren't purged

REMAINING — MUST FIX IN UNITY EDITOR (prefab YAML edits risk corruption):
A. NetworkManager prefab → ServerListenAddress: change 127.0.0.1 to 0.0.0.0 (BLOCKER: external clients literally cannot connect without this)
B. NetworkManager prefab → EnableTimeResync: enable (prevents clock drift on real networks)
C. NetworkManager prefab → ConnectionApproval: enable + set MaxConnectedPlayers (prevents unlimited connections)
D. Player prefab → Rigidbody → Interpolation: set to Interpolate (smoother local movement)

KNOWN REMAINING ISSUES (not yet fixed, lower priority):
- Late-join hydration capped at 512 demolition records — late joiners after heavy destruction see stale state. Needs architectural redesign (snapshot-based hydration instead of event replay). Larger effort.
- Fragment count mismatch between host/client leaves invisible debris (no placeholder creation for unmatched host fragments)
- DemolitionReplicator._demolitionHistory.RemoveAt(0) is O(n) — should use Queue/circular buffer
- No spawn point randomization (all players overlap at origin)
- RocketLauncher.cs is an empty stub
- Client-authoritative movement has no server validation (acceptable for trusted co-op, not for public release)

NOTHING HAS BEEN COMMITTED. All changes are unstaged working directory modifications.
